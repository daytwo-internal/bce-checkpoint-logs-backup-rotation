Check Point FTP Credential


--------Input configuration-------
fields:
  - id: ftp_user
    type: string
    label: FTP Username
  - id: ftp_password
    type: string
    label: FTP Password
    secret: true
required:
  - ftp_user
  - ftp_password

------------------------------

Injector configuration


env:
  CHKP_FTP_USER: '{{ ftp_user }}'
  CHKP_FTP_PASSWORD: '{{ ftp_password }}'

  
----------------------------------

